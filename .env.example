# Build-time only: set this to enable Thick mode in the bridge image.
# Leave blank to build a Thin-only bridge.
INSTANT_CLIENT_URL=

# Hub AP gateway. docker-compose.override.yml publishes MQTT here.
AP_GW_IP=10.42.0.1
COMPOSE_PROJECT_NAME=presence-logger


# Hub AP gateway. docker-compose.override.yml publishes MQTT here.
# Written by scripts/bootstrap/60-stack.sh from site.env.
AP_GW_IP=10.42.0.1
COMPOSE_PROJECT_NAME=presence-logger
