# レトロフィットIoT_活動報告 ノート原稿

<!-- docs/slides/export_notes.py で生成。直すときは build_deck.py のノートを直して作り直すこと -->

全体 5698字 ／ 15.0 〜 19.0 分（基準 17.3 分・間や切替は含まない）

---

## p1 レトロフィットIoT 活動報告

*373字・目安 1.1 分*

レトロフィットIoTの活動報告をいたします。

はじめに一点、先ほどご説明した SpiderLink との関係を申し上げます。データベースに集めて活かしていきたい、という狙いは同じです。違うのは、担当する区間で、SpiderLink は、テキストファイルが手元にある状態から、データベースへ入れるところを受け持ちます。今回お話しするのは、工場ネットワークにもつながっていない設備から、データを生み出して、データベースに蓄積するというところになります。届いた先でデータを活かしていく、という方向は同じだとお考えください。

レトロフィットとは、いまある古い設備をそのままにして、データを取り出すという意味です。

今回は、稼働中の設備に一切手を入れずに、その稼働状況と人のありなしなどを自動でデータベースに記録する仕組みをつくり、現場で動かすところまで確認しました。

## p2 目次

*（ノートなし）*

## p3 背景

*240字・目安 0.7 分*

まず背景です。

データの使い方は、収集、蓄積、活用の三層で整理できます。ピラミッドの形のとおり、上の層ほど、下の層が揃っていることが前提になります。

なお hint DB は、収集の時点で拠点・ライン・工程・時刻まで決めた形で書き込みます。マスタとデータを一緒に登録するので、後から意味を足す工程は要りません。

問題は土台です。蓄積も活用も、正確に収集されたデータが揃って初めて成り立ちますが、その収集が、いまは人の目と手書きに頼っています。今回の取り組みは、この一番下の層を埋めるものです。

## p4 課題と取り組み

*440字・目安 1.3 分*

課題と、それに対する取り組みです。

まず、対象になる設備を切り分けさせてください。ネットワークに繋がっている設備は、PLC や先ほどの SpiderLink で記録が取れます。ここは今回の対象ではありません。

問題は右側です。古い設備や、ネットワークから孤立している設備。稼働記録がどこにも残らない。ここが今回の対象です。

誤解のないように申し上げますが、止めて改造すれば、これらの設備からもデータは取れます。ただ、そのために設備を止め、改造の手配をして、費用をかけるのは現実的ではない、というのが実際のところです。いまは人による目視と手計上に頼っています。

そこでレトロフィットIoT です。対象はカメラに限りませんし、取るデータも稼働状況に限りません。その中で今回は、シグナルタワーを対象に取り組みました。

下段のとおり、これまで人が見て紙に書いて、上長が Excel で集計していたものを、AIカメラがシグナルタワーを認識し、端末がその場で判定し、そのまま hint DB に記録されるようになります。

## p5 システム構成と台数の考え方

*340字・目安 1.0 分*

システム構成と、台数の考え方です。ラズベリーパイという小さなパソコンを使用します。

左が子ラズパイ。測定したい場所ごとに設置する端末で、ここにカメラがついており、AIカメラでシグナルタワーを認識し、データを親ラズパイに送ります。線が示しているとおり、子Piは何台でも1台の親に集まります。これが1対多の関係です。

真ん中が親ラズパイ。子Piを束ねて、代表してデータベースへ書く役です。親ラズパイは、アクセスポイントを作って子ラズパイと接続し、hint DB とは、未検疫帯WiFiでつながっています。この親も1台に限りません。A棟、B棟というように、拠点ごとに増やせます。親どうしは互いに干渉しません。

右が hint DB です。子Piが何台でも、親が何台でも、行き先はこのテーブルひとつです。

## p6 データフロー

*324字・目安 1.0 分*

データの流れです。横に工程、縦に役割をとってあります。

上の段が子ラズパイです。AIカメラで認識し、CSV に1行書き、MQTT で親ラズパイのブローカーへ送ります。

真ん中が親ラズパイ。受け取ったらまず自分の手元に控え、それから送ります。送ったという実績がないと消しません。

子Piは hint DB に直接触れません。これがこの構成の要点です。

子Piがやっているのは MQTT のブローカーに書き込むだけで、カメラであるかどうかを、この経路は問いません。1行が時刻・拠点・ライン・工程・状態という形に収まるものであれば、AMR でも他のセンサでも、同じ経路に載せられます。今回作ったものは、カメラのアプリではなく、現場からデータベースまでの運搬路だとお考えください。

## p7 取り組み内容：アプリ① 稼働監視（シグナルタワー）

*316字・目安 1.0 分*

一つ目のアプリ、稼働監視です。

設備のシグナルタワーを AI カメラで見ます。このAIカメラに何百枚とシグナルタワーの写真を覚えさせ、色を判断できるように作りこみました。

見分けるのは、点いている、点滅している、消えている、の三つです。

点滅に関しては、点灯・消灯の繰り返しの状態が一定時間続いたときだけ確定させます。こうすることで、一瞬の光の反射やちらつきには反応しません。

確定したらデータを1行書き出します。これだけです。

ここでもう一点、申し上げたいことがあります。見ているのはカメラの映像だけなので、画角にシグナルタワーが入ってさえいれば、1台のカメラで何台の設備でも同時に見られます。設備が増えるたびにカメラを増やす必要はありません。

## p8 取り組み内容：アプリ②（設計中） 在場管理（人検知）

*71字・目安 0.2 分*

（今回は説明を省きます）

人検知のアプリ②は設計中です。稼働監視と同じ経路・同じ表に書き込む設計にしていますが、今回は説明を省いて次に進みます。

## p9 正しい時刻で、正確に記録するための工夫

*489字・目安 1.5 分*

記録が信頼できるものになるように、二つの工夫をしています。

左が、欠落も重複もなく記録するという工夫です。親PiはMQTTブローカーが受け取った時点でまず手元に控え、hint DB へ送り終えるまで消しません。データの中身をもとに一意の番号を振っているので、何度送っても同じ1件として扱われます。そして、既存の表を変えずに1行へ収めます。書き込めたら、受領の返信を出します。

結果として、電源が落ちても、無線が切れても、データベースが止まっていても、記録は1件だけになります。

右が、時刻を合わせてから記録を始めるという工夫です。

親Piは無線を2枚持っています。内蔵の無線は工場ネットワーク専用で、時刻サーバは、この経路の先にあります。もう1枚の USB ドングルは子Pi専用のアクセスポイントで、物理的に別の回線です。そのため、つなぐ順番を決めてあります。まずは、親ラズパイが、時刻サーバと同期をする。

工場網に接続し、時刻を合わせ、それが確認できてから子Piの窓口を開く。子ラズパイは、親ラズパイの時刻を同期して、データを収集する。この順番で、収集するデータの時刻がずれることはありません。

## p10 検証（現場実証）

*248字・目安 0.8 分*

検証です。現場で実際に動かした内容をご報告します。

2026年7月30日、試作の加工機のシグナルタワーを対象に実施しました。子ラズパイは Pi Zero 2 W に AI カメラ、親は Pi 5 です。見たものは信号灯の点灯・点滅・消灯です。

右が実際に撮れた画像です。左が現場のシグナルタワーを AI カメラで撮ったもの、右が hint DB に記録が入ったところです。

結果として、シグナルタワーを判定し、工場ネットワークを経由して、hint DB に記録が入るところまで通りました。欠損がなかったかなどの検証はこれからになります。

## p11 効果

*469字・目安 1.4 分*

効果です。二つに分けてご説明します。

左が、減らせる工数です。ここは設備の稼働にかかわる工数に限ってお話しします。

まず、目視での稼働確認です。稼働状態に変化があると書き留めるという作業がなくなります。こちらはまだ計測できていません。

もうひとつが加工時間の手計上です。各班の作業分析によると、毎日1.5時間から3時間、上長1名あたり週に7.25時間かかっています。これは自動収集に置き換わります。ただしこの7.25時間は、時間計上と加工時間計上を合算した値です。このうち設備分がどれだけかは、これから切り分けます。

右が、できるようになることです。いまは取れていない情報が手に入ります。

設備の実稼働率が、目視や記憶ではなく実測で分かる。どの工程がボトルネックで止まっているかが分かる。遅れがその場で分かるので、早期に気づいてフォローできる。そして、加工機でいうと、加工見積もりを経験と勘から実績データに変えられます。

右側は、今後の連続稼働とデータ蓄積で実現していく部分です。

コストは、最小構成の親1台と子1台で8万1千960円、税別の実費です。

## p12 今後の計画

*586字・目安 1.8 分*

今後の計画です。三つの方向で進めます。

一つ目は横展開です。ただし広げる前に、1台の親に子Piを何台まで安定して繋げられるかを実測します。無線のアクセスポイントの同時接続数がここだけ未計測なので、先に押さえます。そのうえで子Piを増設して対象設備を広げ、別のラインや拠点へ同じ構成で展開します。

二つ目は在場管理の検証です。人のいるいないを判定する仕組みは、いま設計中です。これを実機で動かして、ありなしの判定が現場で使えるかを確認します。

三つ目は用途の拡張です。この経路はカメラ専用ではありません。設備もAMRもロボットも、同じ仕組みで一元的に見られるということです。温度や振動といった他のセンサも同様です。

いずれも、いまの構成のまま足していける形にしてあります。対象を増やすだけなら子Piを足すだけ、拠点が増えれば親を足す、という広げ方ができます。さらに、ラズパイでなくても、この機能を別の miniPC に組み込むことも可能です。

最後に、いまの構成は、工場の社内ネットワークに繋がることが前提になっています。ここをモバイル通信に置き換えられれば、社内ネットワークがない場所でもレトロフィットIoT を使えるようになります。そうなれば、協力会社の設備の稼働状況も同じ仕組みで見えるようになります。これを次の目標として考えています。

以上で発表を終わります。ありがとうございました。

## p13 参考資料

*（ノートなし）*

## p14 何台まで繋げられるか

*372字・目安 1.1 分*

よくいただく質問なので、参考として用意しました。1台の親ラズパイに、子ラズパイを何台まで繋げられるか、です。

まず、設計上は台数を制限していません。MQTT の同時接続数に上限は設けていませんし、データの ID は機器名と行の中身から決まるので、何台増えても重複しません。条件は台数ではなく、拠点・ライン・工程の三つ組が重ならないことだけです。

実務上の上限は、下の表の順に効いてきます。IPアドレスは245台まで配れます。hint DB への書き込みは1件ずつ直列ですが、実測から毎分850件ほど。信号灯の状態変化は1台あたり毎分数回ですから、50台つないでも毎分数百件で、ここは律速になりません。

問題は一番上です。無線のアクセスポイントに同時に何台ぶら下がれるかはドングルのドライバ次第で、ここだけ測れていません。台数を広げる前に、まずここを実測します。

## p15 なぜ MQTT で運ぶのか

*544字・目安 1.6 分*

なぜ MQTT で運んでいるのか、という質問をいただくことがあるので用意しました。

まず前提です。いまの構成でも、親ラズパイは受け取ったデータをまず自分の SQLite に書いてから hint DB へ送っています。つまり親のDBはすでにあります。ですから「MQTT か DB か」という選択ではなく、論点は子から親への運び方だけです。

運び方を MQTT ではなく、親にDBサーバを立てて直接書かせる場合と比べると、違いは四つです。

一つ目、子Pi側に要るものが増えます。Pi Zero 2 W は非力なので、軽さは効いてきます。

二つ目、親側にDBサーバを新設して運用することになります。mosquitto は人検知ですでに動いているので、いまは増設なしで済んでいます。

三つ目、生存監視です。MQTT なら、接続時の通知と異常切断時の遺言が仕組みとして付いてくるので、子Piが生きているかがそのまま分かります。監視画面の一番上のペインはこれで作っています。

四つ目、送り先を変えるときです。いまは子が親の中身を知らないので、書き込み先を変えても子は無改修で済みます。

なお、MQTT でなければならない訳ではありません。HTTP でも同じことはできます。ブローカーが1つ増える、つまり止まると全部止まる、という点は素直なデメリットです。

## p16 SpiderLink との関係

*495字・目安 1.5 分*

冒頭で触れた SpiderLink との関係を、もう少し詳しくご説明します。

上の図をご覧ください。現場でデータが生まれ、工場網まで届き、データベースに入って、集計や活用に回る。この流れを四つに区切ってあります。

SpiderLink が受け持つのは③、テキストファイルをデータベースへ入れるところです。今回作ったのは①から③、現場でデータを生み出して、工場網まで届かせるところです。重なっているのは③だけで、①と②は今回新しく作った部分になります。④の狙いは、どちらも同じです。

なぜ①②を作る必要があったか。理由は三つです。

一つ目、子Piは工場網に繋げません。MAC アドレス許可制の閉域網だからです。子Piが書く CSV も、子Pi用の無線の内側にあるので、工場網の側からは見えません。つまり、ファイルがあってもそこに手が届かない、という状態でした。

二つ目、通信が切れてもデータを失えません。控えに貯めて、復旧後にまとめて送ります。

三つ目、時刻です。子Piが刻んだ取得時刻をそのまま記録するので、時刻が合っていなければ記録の意味がなくなります。

この三つを解いた部分が、今回の取り組みだとお考えください。

## p17 運用と見える化

*194字・目安 0.6 分*

参考として、日々の運用です。

現場でしていただく操作は、デスクトップのアイコンを押すだけです。「接続」で工場網につながり、時刻を合わせ、記録が始まります。「切断」で記録を止めて、ふだんのネットワークに戻ります。

入った記録は「直近30件の記録」でその場で確認できます。

右は監視画面です。子Piが生きているか、親Piに届いているか、hint DB に入ったか、を1画面で確認できるようにしています。

## p18 機器構成と実費内訳

*197字・目安 0.6 分*

機器の内訳です。

子ラズパイ1台分が、本体1万3千460円、AI カメラ1万6千50円、小型のタッチ画面2千680円で、小計3万2千190円です。

親ラズパイ1台分が、スターターキット4万7千40円と USB 無線子機2千730円で、小計4万9千770円です。

合計で8万1千960円、税込みで9万156円になります。

子Piを1台増やすごとに3万2千円ほどで対象設備を広げられる、という増やし方ができます。

## p19

*（ノートなし）*
